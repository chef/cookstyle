language: ruby
cache: bundler
sudo: false

branches:
  only:
    - master

# Early warning system to catch if Rubygems breaks something
before_install:
  - gem update --system

rvm:
  - 2.3.8
  - 2.4.5
  - 2.5.3
