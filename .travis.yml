language: ruby
cache: bundler
dist: trusty
sudo: false

branches:
  only:
    - master

# Early warning system to catch if Rubygems breaks something
before_install:
  - gem update --system

rvm:
  - 2.3.4
  - 2.4.1
